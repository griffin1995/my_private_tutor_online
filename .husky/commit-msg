#!/usr/bin/env sh

# CONTEXT7 SOURCE: /vercel/next.js - Commit message validation for development workflows
# DEVELOPMENT STANDARDS REASON: Enforce consistent commit messages for better development tracking

. "$(dirname -- "$0")/_/husky.sh"

echo "📝 Validating commit message format..."

# Use commitlint for commit message validation
npx --no-install commitlint --edit "$1"