# DEBUG CONFIGURATION - MY PRIVATE TUTOR ONLINE
# Copy this to .env.local to enable debug mode

# Main debug toggle - set to 'true' to enable debug mode
NEXT_PUBLIC_DEBUG_MODE=true

# Visual debug features
NEXT_PUBLIC_DEBUG_BORDERS=true
NEXT_PUBLIC_DEBUG_LABELS=true
NEXT_PUBLIC_DEBUG_DATA_ATTRS=true

# Console logging
NEXT_PUBLIC_DEBUG_LOGGING=true

# Visual customization
NEXT_PUBLIC_DEBUG_COLOR_SCHEME=rainbow
NEXT_PUBLIC_DEBUG_BORDER_WIDTH=2
NEXT_PUBLIC_DEBUG_LABEL_POSITION=top-left

# QUICK SETUP:
# 1. Copy this file to .env.local
# 2. Restart your development server
# 3. Debug features will be available

# RUNTIME CONTROLS:
# - Use the debug panel (top-left corner when enabled)
# - Keyboard shortcut: Ctrl+Shift+D to toggle debug mode
# - All features can be controlled at runtime via the debug panel

# PRODUCTION NOTE:
# Debug features automatically disable in production builds
# regardless of these environment variables for security